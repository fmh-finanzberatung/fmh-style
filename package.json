{
  "name": "fmh-style",
  "version": "0.0.4",
  "description": "FMH SCSS styles",
  "main": "index.js",
  "repository": "https://github.com/fmh-finanzberatung/fmh-style.git",
  "author": "FMH-Finanzberatung <info@fmh.de>",
  "license": "MIT",
  "scripts": {
    "build": "yarn build:css && yarn build:kss",
    "build:css": "rimraf dist/* && node-sass src -o dist",
    "build:kss": "yarn build:css && kss --config config/kss-config.json",
    "dev": "yarn build:kss && node server.js",
    "watch:scss": "yarn build:css && node-sass src/ -wor dist"
  },
  "devDependencies": {
    "chokidar": "^3.4.0",
    "express": "^4.17.1",
    "kss": "^3.0.1",
    "node-sass": "^4.13.0",
    "nodemon": "^2.0.1",
    "rimraf": "^3.0.0",
    "typeface-roboto": "^0.0.54",
    "websocket": "^1.0.31"
  }
}
